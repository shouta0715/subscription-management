/* eslint-disable no-restricted-imports */
import NativeContextMenu from "react-native-context-menu-view";
import { withUniwind } from "uniwind";

const ContextMenu = withUniwind(NativeContextMenu);

export { ContextMenu };
